#include<stdio.h>
#include<float.h>

int main()
{
	printf("The size of smallest positive non-zero value of type float is %.3e\n",DBL_MIN);
	printf("The size of largest value of type float is %.3e\n",DBL_MAX);
	return 0;
}
